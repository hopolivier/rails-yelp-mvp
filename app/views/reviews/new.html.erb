<!-- new.html.erb -->
<%= simple_form_for([@restaurant, @review]) do |f| %>
  <%= f.error_notification %>

  <%= f.input :content, label: "Your appreciation" %>
  <%= f.input :rating, collection: 1..5, prompt: "Nb of stars", selected: 3 %>
  <%= f.button :submit %>
<% end %>
